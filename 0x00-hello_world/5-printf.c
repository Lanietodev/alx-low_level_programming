#include <stdio.h>
/**
 * main - prints a string of texts
 *
 * Return: 0 on successful completion, or a non-zero value on error.
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
