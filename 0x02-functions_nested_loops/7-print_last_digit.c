#include "main.h"
/**
 * print_last_digit - entry point
 *
 * @int: param type for _abs
 * @n: value to check
 * Return: last digit (success)
*/
int _abs(int);
int print_last_digit(int n)
{
return (_abs(n) % 10);
}
